<%= form_with model: @recruitment_category, url: recruitment_path(@recruitment.id), method: 'patch', local: true do |f| %>
  <%= render 'error_messages', model: @recruitment_category %>
  <div>
    タイトル
  </div>
  <%= f.text_field :title, placeholder: "100字以内" %>
  <div>
    募集カテゴリ
  </div>
  <%= f.select :category_name, [["対戦相手", "対戦相手"], ["助っ人", "助っ人"]], include_blank: "選択してください" %>
  <div>
    レベル
  </div>
  <%= f.collection_select(:level_id, Level.all, :id, :name, {}) %>
  <div>
    定員
  </div>
  <%= f.collection_select(:capacity_id, Capacity.all, :id, :name, {}) %>
  募集カテゴリが「対戦相手」の場合は、1を選択してください
  <div>
    開催エリア
  </div>
  <%= f.collection_select(:prefecture_id, Prefecture.all, :id, :name, {}) %>
  <div>
    球場
  </div>
  <%= f.text_field :ball_park, placeholder: "例)夢の島球場12面" %>
  <div>
    開催日時
  <div>
  <%= f.date_field :event_date, placeholder: "年/月/日" %>
  <%= f.collection_select(:start_time_id, StartTime.all, :id, :name, {}) %>
  -
  <%= f.collection_select(:end_time_id, EndTime.all, :id, :name, {}) %>
  <div>
    募集期限
  </div>
  <%= f.date_field :recruitment_deadline, placeholder: "年/月/日" %>
  <div>
    本文
  </div>
  <%= f.text_area :recruitment_text, placeholder: "1000字以内" %>
  <%= f.submit "募集を編集する" %>
<% end %>
<div>
  <%= link_to "戻る", :back %>
</div>