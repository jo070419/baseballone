応募メッセージ
<%= form_with model: @apply_message, url: recruitment_applies_path(@recruitment.id), local: true do |f|%>
  <%= f.text_area :text, placeholder: "500字以内" %>
  <%= f.submit "応募メッセージを送信する" %>
<% end %>
<%= link_to '戻る', recruitment_path(@recruitment.id) %>